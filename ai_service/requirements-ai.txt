# FastAPI AI Microservice — Dependencies
# Install separately from the main Django app:
#   cd ai_service && pip install -r requirements-ai.txt

# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Data validation
pydantic>=2.0

# HTTP client (async Groq calls)
httpx>=0.27.0

# Anthropic Claude SDK (optional — only needed if using provider=anthropic)
anthropic>=0.40.0

# Environment variables
python-dotenv

# Error tracking (optional — reads SENTRY_DSN env var)
sentry-sdk[fastapi]>=2.0.0
